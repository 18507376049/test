<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE configuration
    PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
	<properties resource="conn_db.properties"/>
	<typeAliases>
		<package name="com.bookshopping.pojo"/>
	</typeAliases>
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC"/>
			<dataSource type="UNPOOLED">
				<property name="driver" value="${conn_driverclass}" />
				<property name="url" value="${conn_url}" />
				<property name="username" value="${conn_username}" />
				<property name="password" value="${conn_password}"/>
			</dataSource>
		</environment>
	</environments>
	<mappers>
		<mapper resource="bookTypeInfoMapper.xml"/>
		<mapper resource="userInfoMapper.xml"/>
	</mappers>
</configuration>
